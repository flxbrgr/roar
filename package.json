{
  "name": "tournify-variant",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "client",
    "server"
  ],
  "overrides": {
    "vite": "^7.1.10"
  },
  "scripts": {
    "install:all": "npm install --workspaces",
    "build": "npm run build --workspaces",
    "lint": "npm run lint --workspaces",
    "test": "npm run test --workspaces",
    "dev": "npm run dev --workspace server"
  }
}
